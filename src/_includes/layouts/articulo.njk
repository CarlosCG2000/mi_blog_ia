<!DOCTYPE html>
<html lang="es" class="scroll-smooth">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{{ title }} | Mi Blog Asombroso</title>
        <script src="https://cdn.tailwindcss.com"></script>
        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;700;800&display=swap" rel="stylesheet">
        <style>
            body {
                font-family: 'Inter', sans-serif;
                background-color: #FDFBF8;
                color: #333;
            }
            .prose h1,
            .prose h2,
            .prose h3 {
                color: #422006;
            }
            .prose a {
                color: #D97706;
            }
            .prose a:hover {
                color: #B45309;
            }
            .prose blockquote {
                border-left-color: #FBBF24;
            }
            .prose code {
                color: #B45309;
                background-color: #FFFBEB;
                padding: 2px 4px;
                border-radius: 4px;
            }
            .prose pre > code {
                padding: 0;
            }
        </style>
    </head>
    <body class="antialiased">
        <header class="bg-white/80 backdrop-blur-lg sticky top-0 z-50 shadow-sm">
            <nav class="max-w-5xl mx-auto px-6 py-4 flex justify-between items-center">
                <a href="/" class="text-2xl font-bold text-amber-900 hover:text-amber-600 transition-colors">Mi Blog Asombroso</a>
                <a href="/" class="text-amber-700 hover:text-amber-900 font-semibold">← Volver al inicio</a>
            </nav>
        </header>
        <main class="max-w-5xl mx-auto px-6 py-12 md:py-16">
            <article class="bg-white p-8 md:p-12 rounded-lg shadow-lg prose lg:prose-xl max-w-none">
                <h1>{{ title }}</h1>
                {% if date %}
                    <p class="text-base text-gray-500">
                        Publicado el
                        <time datetime="{{ date }}">{{ date.toLocaleDateString('es-ES', { year: 'numeric', month: 'long', day: 'numeric' }) }}</time>
                    </p>
                {% endif %}
                {{ content | safe }}
            </article>
        </main>
        <footer class="text-center py-8 text-gray-500 text-sm">
            <p>&copy; {{ "now" | date("yyyy") }} Mi Blog Asombroso. Todos los derechos reservados.</p>
        </footer>
    </body>
</html>